# 📌 待修改问题与后续迭代规划 (Known Issues & Task Backlog)

## ✅ 已解决事项 (Completed)
- `[x]` **PDF 电子版解析**：通过 `pdf-extract` 实现了矢量文本提取。
- `[x]` **PDF 纯图片扫描件 & 图像资产解析**：通过 macOS 原生 `Vision.framework` 本地 OCR 模块，实现了扫描件 PDF（如 `14.pdf`）与 PNG/JPG/WEBP 图片中汉字与英文的毫秒级离线提取。
- `[x]` **Word (DOCX) 文本剥离**：通过 `zip` 结合 XML 解析实现了文档文本提取。
- `[x]` **AI 智能批量重命名与列表跳帧 Bug**：重构了「半映射半实际」虚拟数据抽象层，并修复了最近文件一致性哈希 ID 算法。
- `[x]` **代码工程文件干扰**：全量过滤 `.js`, `.py`, `.rs`, `.json`, `node_modules` 等开发工程文件，保障智能分类与检索纯净。
- `[x]` **文件列表页多维度交互**：实现多选、大小/时间正倒序排列及当前列表动态格式筛选。
- `[x]` **混合搜索引擎重构**：基于 Marvis 产品逻辑，引入 RRF (倒数排名融合) 算法，结合 FTS5 全文搜索与 sqlite-vec 向量检索，实现双路召回与强健的平滑降级机制。

- `[x]` **Excel (XLS/XLSX/CSV) 多 Sheet 网格化交互预览**：通过 Rust `calamine` 结合前端网格组件，实现了可滚动、支持多 Sheet 选项卡切换的本地表格渲染。
- `[x]` **图片高级预览 (缩放 / 1:1 / 适应窗口 / 剪裁 / 90°旋转 / OCR划词复制)**：暗色半透明胶囊 Bar 打造，支持 1:1 原始尺寸、自适应窗口居中与交互式图像裁剪。

## ⏳ 进行中 / 待优化事项 (Backlog)
- `[ ]` **多模态 Vision LLM 集成**：后续可选引入轻量级 Vision LLM（如 `qwen2.5-vl`）对极复杂手写体或多图表排版进行深度意图抽取。
