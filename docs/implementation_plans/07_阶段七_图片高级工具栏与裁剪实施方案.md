# 阶段七实施方案：图片暗色悬浮工具栏 (1:1 / 适应窗口 / 裁剪)

## 1. 架构目标
- 参照用户设计的暗色胶囊悬浮 Bar，集成 `[-]` `[百分比下拉]` `[+]` `[1:1原始大小]` `[适应窗口]` `[90°旋转]` `[裁剪]` 控制工具。
- 实现交互式图片裁剪，并提供物理文件保存 (`crop_image_native`) 能力。

## 2. 计划步骤
- [ ] 构建 `PreviewerView.jsx` 中的暗色悬浮胶囊工具栏 UI。
- [ ] 实现 `1:1` 100% 自然像素模式与 `适应窗口` 比例居中模式。
- [ ] 添加缩放百分比选择下拉菜单 (`25%`, `50%`, `75%`, `100%`, `150%`, `200%`, `适应`)。
- [ ] 实现前端 8 点手势拖拽裁剪选区框。
- [ ] 在 `src-tauri/src/lib.rs` 中新增 `crop_image_native` 指令实现物理图像裁剪。
