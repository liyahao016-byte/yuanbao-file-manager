# 2026-08-30 工作日志

## 归档功能修复 — 去除所有 mock 数据，接入真实后端

### 后端新增（archive.rs）
- `query_history_tags`：从 archives 表聚合所有历史标签，按使用频次降序返回 top 30
- `query_recent_files`：从 files 表按 modified_timestamp 降序返回最近文件列表，自动过滤磁盘上已不存在的文件
- `ai_analyze_files`：接收文件路径列表，调用 file_parser 读取文件内容，拼接 prompt 调用 Ollama qwen2.5:32b 生成归档标题/产出/标签，含完整降级兜底

### 前端修复
- **QuickArchiveModal**：historyTags 和 recentFiles 从硬编码改为 useEffect + invoke 从后端真实获取
- **ArchiveTimelineView**：删除 MOCK_ARCHIVES 常量，去掉所有 mock 回退逻辑，数据库为空时展示空状态引导
- **ArchiveFromClusterModal**：AI 预填从 setTimeout(2000) 假延迟改为 invoke('ai_analyze_files') 真实 Ollama 调用，historyTags 改为从 DB 读取

### lib.rs
- invoke_handler 新增注册 3 个命令：query_history_tags、query_recent_files、ai_analyze_files

### 验证
- Rust cargo check 通过（无新增 warning）
- 前端 vite build 通过
