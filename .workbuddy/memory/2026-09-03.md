# 2026-09-03 工作日志

## 产品需求看板策划文档
- 基于现有归档时间线功能，输出了完整的"产品需求看板"重构策划文档 V1.0
- 核心变更：从"事务归档"扁平结构重构为"需求→节点"二级树形结构
- 新增 demands 表（需求层）+ archives 表扩展（demand_id / node_type / attachments / is_key_conclusion）
- 设计了四列看板视图（规划中/进行中/Hold/已完成）替代原时间线作为默认视图
- 设计了需求详情侧边面板（状态速览+文档链接+节点时间线）
- QuickArchiveModal 改造为节点记录表单（项目→所属需求，新增节点类型/关键结论标记）
- 全局搜索增强：关键结论加权 + 面包屑定位到节点级
- 现有功能删改映射：16 项功能逐条标注保留/改造/增强/废弃
- 五阶段实施路线图：数据层→看板→详情面板→表单改造→搜索增强
- 输出文件: output/产品需求看板策划文档.html

## 产品需求看板全量开发完成
按照 PRD 五阶段全部实施完毕，前后端编译均通过。

### Phase 1: 数据层
- `db.rs`：新增 `demands` 表（15列）+ `demand_doc_links` 表（7列）+ 相关索引；`archives` 表 ALTER TABLE 新增 4 字段
- `archive.rs`：新增 8 个 Tauri 命令（create/update/query/delete_demand、add/remove_demand_doc_link、query_demand_nodes、migrate_projects_to_demands）+ 5 个数据结构 + `maybe_set!` 宏
- `lib.rs`：注册 8 个新命令
- 修复两处 `E0063` 编译错误（`parse_md_blocks` 和 `query_archives_internal` 缺少新字段）

### Phase 2: 看板视图 — DemandKanbanView.jsx（新建, ~437 行）
- 四列看板（规划中/进行中/Hold/已完成），HTML5 拖拽跨列变更状态
- 列底部内联新建表单（需求名+优先级+阶段）
- 顶部搜索框 + P0/P1/P2 筛选 + 看板/时间线视图切换
- 统计条（总需求/进行中/有卡点/已完成）
- 点击卡片打开 DemandDetailPanel 侧边面板

### Phase 3: 需求详情面板 — DemandDetailPanel.jsx（新建, ~530 行）
- 三区结构：A 状态速览 / B 关联文档 / C 推进时间线
- A区：2×2 网格可编辑字段（阶段/状态/目标日期/优先级）+ 跨栏卡点/下一步
- B区：文档链接列表 + 添加表单，5 种文档类型颜色标签
- C区：节点时间线竖线+圆点，5种节点类型颜色，关键结论黄底星标
- 面板可拖拽调宽（340-700px），底部二次确认删除

### Phase 4: QuickArchiveModal 表单改造
- 新增"所属需求"下拉选择（从 `query_demands` 加载选项列表）
- 新增"节点类型"选择（进展/结论/文档/里程碑/卡点）
- 新增"关键结论"复选框（星标样式）
- `handleSubmit` 传出 `demandId`/`nodeType`/`isKeyConclusion`
- App.jsx `handleArchiveConfirm` 将新字段传入 `archive_file` invoke

### Phase 5: 集成 + 编译验证
- App.jsx：import DemandKanbanView，侧边栏"归档时间线"改名"需求看板"，路由渲染 DemandKanbanView
- `onOpenArchiveModal` 支持 demandCtx 传入 demandId
- QuickArchiveModal 新增 `demandContext` prop
- **编译结果**：Rust cargo check 0 errors，Vite build 0 errors（727KB）

## 看板视图优化
- **布局**：移除时间线 tab；进行中列 flex:2.5 占主视窗，其他三列 flex:1 紧凑展示
- **高度自适应**：非主列 maxHeight:420px + 溢出滚动，主列自由伸展
- **卡片增强**：新增阶段进度条（6阶段百分比）、下一步todo始终展示（含日期提取）、预期合流/上线时间（🎯图标）
- 编译：Vite build 0 errors（728KB）

## 看板视觉效果全面优化（设计师视角）
- **全局底色**：页面背景从白色改为 #f8f9fb 浅灰，增加层次深度
- **顶部区域**：标题图标化（渐变方块图标 + 副标题），搜索框聚焦动效（扩宽+光圈）
- **统计卡片化**：四个统计数据改为独立卡片（渐变背景+图标+大号数字），取代纯文字排列
- **列设计重构**：列背景色大幅降噪（近白色调），列头加 icon + 渐变彩色条装饰，列容器圆角加大（14px）
- **空状态设计**：空列不再显示空白，展示 emoji 插画 + 提示文案
- **卡片精致化**：左侧主题色条（3px）、进度条改胶囊渐变+内阴影、hover 上浮效果（translateY -1px + 大阴影）
- **信息标签化**：目标日期/节点数改为胶囊标签、卡点改为带背景警告块
- **新建按钮增强**：创建按钮使用渐变背景+投影
- **筛选器优化**：优先级筛选改为 segment 容器样式（灰底白激活卡片）
- 编译：Vite build 0 errors（733KB）

## 归档表单简化 + 关联文档合并到节点
- **取消所有必填项**：去除标题和内容的 `*` 必填标记，按钮不再 disabled 校验空字段
- **删除顶部标题/项目/优先级/耗时区域**：原来作为表单顶部的"标题*"+"项目+优先级+耗时"行被移除
- **"内容"作为主输入**：原"关键产出"改名为"内容"，成为表单第一个字段（自动聚焦）
- **标题/项目/优先级**降级为可选辅助字段（一行三列，不再显眼）
- **DemandDetailPanel 移除独立 B 区"关联文档"**：删除手动添加文档名+URL+类型的表单
- **附件随节点自动展示**：时间线节点展开后显示 linkedFiles 附件列表，图片/文档均可点击打开
- **节点行附件标记**：未展开时也能看到 📎 数量小标签
- 编译：Vite build 0 errors（730KB）

## 看板布局重构 + 进度-状态映射 + 新建需求弹窗
- **2×2 网格布局**：看板改为 grid 2col 布局（上排：规划中 | 进行中，下排：Hold | 已完成），参考截图设计
- **取消各列底部新建按钮**：统一在顶部栏新增"+ 新建需求"渐变按钮，弹窗式创建
- **新建需求弹窗**：支持选择需求名称、当前进度（7阶段）、优先级、看板位置；进度自动映射状态
- **进度-看板映射关系**：需求调研→规划中、方案设计~灰度上线→进行中、全量上线→已完成、Hold 不映射特定进度
- **拖拽逻辑增强**：拖入规划中→phase 自动改为需求调研；拖入已完成→phase 改为全量上线；拖入进行中→若原 phase 无效则改为方案设计；拖入 Hold→保持原 phase
- **阶段列表扩展**：PHASES 增加"全量上线"（7个阶段），三个文件同步更新
- **QuickArchiveModal 进度选择**：将原"标题+项目+优先级"行替换为"当前进度"按钮组，让节点记录标记产出时所处阶段
- **DemandDetailPanel 时间线倒序**：节点列表改为 archivedAt 时间倒序排列
- **NODE_TYPES 扩展**：增加7个阶段类型到 NODE_TYPES 映射，节点时间线可展示"需求调研""开发联调"等阶段图标
- 编译：Vite build 0 errors（733KB）
